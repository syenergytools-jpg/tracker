# Same Supabase project the AMS uses, same env var names as the AMS's
# Next.js .env.local so you can copy the values straight across.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
